name: CI Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-windows:
    name: Windows build
    runs-on: windows-latest

    steps:
    - uses: actions/checkout
    - name: make
      run: make -f fbdump.mak windows
